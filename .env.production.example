# ================================
# Configuration Production - Cloud Run
# ================================

# Base de données Cloud SQL
# Utilisez la connexion via socket Unix pour Cloud SQL
DB_HOST=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
# Ou utilisez l'IP privée si vous avez configuré le VPC
# DB_HOST=10.x.x.x
DB_USER=association_user
DB_PASSWORD=VOTRE_MOT_DE_PASSE_SECURISE
DB_NAME=association_db
DB_PORT=3306

# Serveur
PORT=8080
NODE_ENV=production

# JWT - IMPORTANT: Utilisez un secret fort et unique !
# Générez avec: openssl rand -base64 64
JWT_SECRET=VOTRE_SECRET_JWT_SUPER_SECURISE_GENERE_ALEATOIREMENT
JWT_EXPIRES_IN=7d

# CORS - Mettez l'URL de votre domaine
CORS_ORIGIN=https://votre-domaine.com

# ================================
# Pour Cloud Run, configurez ces variables via:
# 1. Console GCP > Cloud Run > Variables d'environnement
# 2. Ou via Secret Manager pour les valeurs sensibles
# ================================
